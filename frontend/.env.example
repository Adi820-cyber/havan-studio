# ─── HAVAN Studio Frontend ─── #
# Copy to .env.local and fill in values

# API Base URL — only needed in production builds.
# In development the Vite proxy routes /api to localhost:3000 automatically.
# VITE_API_BASE_URL=https://api.havan.studio
